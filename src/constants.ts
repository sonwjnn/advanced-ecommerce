// DEFAULT_LIMIT - Default number of items to fetch per page in paginated queries
export const DEFAULT_LIMIT = 8;

// PLATFORM_FEE_PERCENTAGE - Platform fee percentage
export const PLATFORM_FEE_PERCENTAGE = 10;
